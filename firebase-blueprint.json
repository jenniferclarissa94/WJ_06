{
  "entities": {
    "UserInteraction": {
      "title": "UserInteraction",
      "description": "A saved prompt and Gemini response interaction for a user",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the interaction"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who initiated this interaction"
        },
        "prompt": {
          "type": "string",
          "description": "The user's query or prompt to the Gemini assistant"
        },
        "response": {
          "type": "string",
          "description": "The response generated by Gemini"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of when the interaction occurred"
        }
      },
      "required": ["id", "userId", "prompt", "response", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}/chat_history/{interactionId}": {
      "schema": "UserInteraction",
      "description": "Stores user chat queries and Gemini assistant answers, isolated strictly to the user"
    }
  }
}
